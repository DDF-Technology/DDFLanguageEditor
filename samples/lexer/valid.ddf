@@'Console'

// Sorgente lessicalmente valido.
main() out int
{
    string message << "Ciao DDF";
    int count << 3;

    while(count > 0)
    {
        message >> Console;
        count--;
    }

    ret count;
}
