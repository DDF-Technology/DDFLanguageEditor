@@'Console'

struct Point
{
    int x;
    int y;
}

main() out int
{
    int[3] values;
    int total << 1 + 2 * 3;

    for(int index; index < 3; index++)
    {
        values[index] << index ^ 2;
        total << total + values[index];
    }

    ret total;
}
